import './bootstrap';
import 'preline';

// Import the stepper
import { HSStaticMethods } from "@preline/stepper";

// Initialize Preline
document.addEventListener('DOMContentLoaded', () => {
    // Initialize Preline
    window.HSStaticMethods.autoInit();
});
